- **本地构建触发**：通过 GitHub UI 手动触发 `Build Local` 工作流，执行 `dotnet publish` 生成 win-x64 自包含包并上传为为期 7 天的构建产物。
- **测试执行命令**：CI 环境中通过 `dotnet restore` 和 `dotnet test` 针对 `tests/MacroDeck.Tests/MacroDeck.Tests.csproj` 运行单元测试。