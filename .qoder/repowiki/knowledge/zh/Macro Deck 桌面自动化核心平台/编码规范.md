- 全局启用 C# 可空引用类型（Nullable enable）与隐式 using，并在 Directory.Build.props 中统一屏蔽特定编译器警告（WFO1000）。
- 遵循 MVVM 模式组织代码，在 GUI 模块中严格区分 Views（视图）、ViewModels（视图模型）与 CustomControls（自定义控件）。
- 采用 Serilog 结构化日志规范，通过 `Log.ForContext<T>()` 在所有核心类中注入上下文感知的日志记录器。