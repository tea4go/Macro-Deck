- 以 PowerShell 脚本 `update-macrodeck-local.ps1` 为单一入口，封装完整的 CI/CD 闭环逻辑。
- 依赖 GitHub CLI (`gh`) 进行远程交互，包括触发 `build-local.yml` 工作流、轮询运行状态及下载 `macro-deck-build` 构件。
- 采用“停止进程 -> 覆盖核心文件（跳过用户数据目录） -> 重启进程”的原子化更新策略，确保本地环境的一致性与安全性。