- 采用扁平化测试结构，每个测试类（如 `ConditionActionTests`、`ConvertNameStringTests`）直接对应主项目中的一个核心业务组件。
- 通过 `ProjectReference` 直接依赖 `src/MacroDeck`，利用全局引用文件 `Usings.cs` 统一导入 `NUnit.Framework` 以简化代码。
- 广泛使用数据驱动测试模式，通过 `[TestCase]` 和 `[TestCaseSource]` 提供多组输入输出数据，覆盖边界情况与回归场景。