- 采用模块化工作流设计，`ci.yml` 作为入口协调器，通过 `workflow_call` 触发 `tests.yml` 执行单元测试。
- 构建环境锁定为 `windows-latest`，使用 `cmd` shell 执行 .NET CLI 命令进行依赖还原与测试。
- `build-local.yml` 提供手动触发的自包含发布构建，产出物通过 `actions/upload-artifact` 归档。
- `release.yml` 定义自动化变更日志分类规则，按标签（feature, bug 等）组织发布说明。