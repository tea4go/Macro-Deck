- 所有文件路径引用必须通过 `ApplicationPaths` 类的静态属性获取，严禁在代码中硬编码物理路径字符串。
- 核心子系统的初始化遵循严格的顺序依赖链：路径初始化 -> 日志配置 -> 主配置加载 -> 语言环境 -> 各功能管理器 -> 网络服务启动。
- 配置对象修改后需显式调用持久化方法（如 `Save`）同步至 JSON 文件，且敏感字段（如 SSL 私钥）在落盘前必须经过加密处理。